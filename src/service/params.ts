export const keySchema = { type: "string", convert: true };

export const valueSchema = [{ type: "string" }, { type: "number" }, { type: "boolean" }, { type: "object" }];

export const patternSchema = { type: "string", optional: true };
